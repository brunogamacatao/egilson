<h2>Entrar</h2>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div><%= f.label "Nome de usuário (nick)" %><br />
  <%= f.text_field :username, :autofocus => true %></div>

  <div><%= f.label "Senha" %><br />
  <%= f.password_field :password %></div>

  <% if devise_mapping.rememberable? -%>
    <div><%= f.label "Lembrar de mim" %> <%= f.check_box :remember_me %></div>
  <% end -%>
  <br/>
  <div><%= f.submit "Entrar", :class => "button round" %></div>
<% end %>

<%= render "devise/shared/links" %>

<h5 class="text-right">
  Ou&nbsp;&nbsp;
  <%= link_to "Entre pelo Facebook", user_omniauth_authorize_path(:facebook), :class => "button small" %>
</h5>